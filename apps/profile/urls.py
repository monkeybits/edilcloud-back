# coding: utf-8

from django.conf.urls import url, include


urlpatterns = [
    # url(r'^api/backend/', include('profile.api.backend.urls', namespace='api_backend')),
    # url(r'^api/frontend/', include('profile.api.frontend.urls', namespace='api_frontend')),
]
